export function SetupNotice() { return <main className="mx-auto max-w-xl px-6 py-20"><p className="text-sm font-semibold tracking-[.2em] text-[#4c7a64]">SUNRISE OS</p><h1 className="mt-3 text-3xl font-semibold">Conecte o Supabase para começar.</h1><p className="mt-4 leading-7 text-slate-600">Copie <code>.env.example</code> para <code>.env.local</code>, preencha as duas variáveis públicas e aplique a migration em <code>supabase/migrations</code>. Nenhum segredo deve ser incluído aqui.</p></main>; }
